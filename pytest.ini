[pytest]
markers =
    integration: 需要 WEIQUAN_TEST_DB_NAME 与可连通的 PostgreSQL
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = module
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
